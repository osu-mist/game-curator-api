DROP TABLE DEVELOPERS;
DROP TABLE VIDEO_GAMES;
DROP TABLE REVIEWS;

CREATE TABLE DEVELOPERS (
  ID NUMBER GENERATED ALWAYS AS IDENTITY (START WITH 1 INCREMENT BY 1) NOT NULL,
  NAME VARCHAR2(255) NOT NULL,
  WEBSITE VARCHAR2(255),
  PRIMARY KEY(ID)
);

INSERT INTO DEVELOPERS (NAME) VALUES ('BETHESDA');

CREATE TABLE VIDEO_GAMES (
  ID NUMBER GENERATED ALWAYS AS IDENTITY (START WITH 1 INCREMENT BY 1) NOT NULL,
  DEVELOPERID NUMBER,
  NAME VARCHAR2(255) NOT NULL,
  SCORE FLOAT,
  RELEASEDATE DATE NOT NULL,
  PRIMARY KEY (ID),
  FOREIGN KEY(DEVELOPERID) REFERENCES DEVELOPERS(ID)
);

INSERT INTO VIDEO_GAMES (DEVELOPERID, NAME, RELEASEDATE) VALUES (1, 'FALLOUT 3', TO_DATE('2008/10/15', 'YYYY/MM/DD'));

CREATE TABLE REVIEWS (
  ID NUMBER GENERATED ALWAYS AS IDENTITY (START WITH 1 INCREMENT BY 1) NOT NULL,
  GAMEID INT NOT NULL,
  REVIEWTEXT VARCHAR2(255) NOT NULL,
  SCORE FLOAT NOT NULL,
  REVIEWER VARCHAR2(255) NOT NULL,
  REVIEWDATE DATE DEFAULT SYSDATE NOT NULL,
  PRIMARY KEY(ID),
  FOREIGN KEY(GAMEID) REFERENCES VIDEO_GAMES(ID)
);

INSERT INTO REVIEWS (GAMEID, REVIEWTEXT, SCORE, REVIEWER) VALUES (1, 'BEST GAME EVER', '5', 'BIG BRAIN');
